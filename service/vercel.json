{
	"version": 2,
	"installCommand": "pnpm install",
	"outputDirectory": "build",
	"builds": [
		{
			"src": "./src/index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/api/$1"
		}
	]
}
