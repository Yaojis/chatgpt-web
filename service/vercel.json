{
	"version": 2,
	"installCommand": "pnpm install && pnpm clean && tsup",
	"outputDirectory": "build",
	"builds": [
		{
			"src": "./build/index.mjs",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/api/$1"
		}
	]
}
