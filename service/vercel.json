{
	"buildCommand": "pnpm build",
	"installCommand": "pnpm install",
	"outputDirectory": "build",
	"builds": [
		{
			"src": "./build/index.mjs",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/api/$1"
		}
	]
}
