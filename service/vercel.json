{
	"version": 2,
	"buildCommand": "pnpm build",
	"installCommand": "pnpm install",
	"outputDirectory": "build",
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/api/$1"
		}
	]
}
